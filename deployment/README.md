# deployment/

This directory contains the inference deployment assets:

- `app/main.py`: FastAPI app exposing `/health` and `/predict`
- `requirements.txt`: runtime dependencies for the service container
- `Dockerfile`: image build for Kubernetes deployment

The service expects a JSON model artifact generated by `model/pipeline.py`.
